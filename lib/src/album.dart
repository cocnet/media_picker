class Album {
  final int albumId;
  final String albumName;
  final int count;
  final String thumbPath;
  final String thumbIdentifier;

  Album(this.albumId, this.albumName, this.count, this.thumbPath,
      this.thumbIdentifier);
}
